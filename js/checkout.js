import { db } from './firebase.js';
import { doc,getDoc } from 'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';
const params=new URLSearchParams(location.search);const id=params.get('id');const token=params.get('token');const title=document.querySelector('#checkoutTitle');const body=document.querySelector('#checkoutBody');const money=n=>Number(n||0).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2});const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
(async()=>{if(!id||!token){title.textContent='Invalid payment link';body.textContent='This checkout link is incomplete.';return}try{const snap=await getDoc(doc(db,'paymentIntents',id));if(!snap.exists()||snap.data().token!==token){title.textContent='Payment not found';body.textContent='This payment link is invalid or unavailable.';return}const p=snap.data();title.textContent=`Pay ${p.merchantName||'merchant'}`;body.innerHTML=`<p class="eyebrow">${esc(p.reference)}</p><h2 style="font-size:3rem;margin:.4rem 0">APX ${money(p.amount)}</h2><p>${esc(p.title||'Payment')}</p><p><span class="status sandbox">SANDBOX</span></p><p class="muted">Customer wallet authorization is the next component. No balance will be changed from this public checkout page.</p>`}catch(e){title.textContent='Checkout unavailable';body.textContent=e.message}})();
